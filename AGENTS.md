# 项目接手约定

本文件是后续开发的上下文入口；修改历史见 [CHANGELOG.md](CHANGELOG.md)，本轮完整背景见 [2026-09-12 前端改版记录](docs/2026-09-12-三个计算器前端改版记录.md)。用户新的明确要求优先于这里的历史约定。

## 当前阶段

- 三个计算器的本地前端改版于 2026-09-12 暂时收尾。下次工作按用户的新需求确定范围，不自行推进暂缓项。
- 本轮基于 `baa0b074ceac759a674c27acd56d3f020d95f5fa`，在 `design/mobile-single-point` 分支实施。该信息是阶段快照，接手时先检查当前 Git 状态。
- 本轮收尾时有已修改和未跟踪文件，尚未提交。保留它们，不用整库恢复或清理覆盖本轮成果；只看 `git diff --stat` 会漏掉新文件。

## 已确认的产品约定

- 沿用白底与现有视觉语言，优先手机舒适度，PC 避免标签、数字和控件散得过开。
- 保留原字段、按钮及计算流程的业务顺序。士兵放在英雄之前有游戏依据，不为美观自行重排。
- 英雄最终面板：左侧英雄头像与生命；右侧依次为“攻击/智力”“防御/魔防”“技巧/空位”。手机和 PC 都保持这个对应关系。
- 士兵最终面板的主体是士兵；搭配英雄只作辅助身份说明。
- 全局额外加粗已被用户要求回退。保留当前正常层级，只按后续具体要求调整；红/橙色数值允许适度加粗，长说明仍需易读。
- 白字小数必须保留。展示数值最多两位小数，不补零；原输入公式和编辑中的数值不截断。
- `calculator-display.mjs` 只用于显示，禁止把格式化字符串回写到计算数据。保留现有游戏计算中的取整、百分比单位和内部精度。
- 使用说明整体默认折叠。长设置区默认展开，可手动收起；折叠不能清掉数据。

## 修改边界

- 本轮仅修改三个公开计算器的前端；现有后台管理、鉴权体系和后端/数据库保持原状。计算器不显示登录入口，不代表可以删掉全站登录功能。
- 真实数据调试只使用已授权的读取接口，不写数据库、不修改后端。
- 不读取或输出临时令牌内容，不把凭据写入文档、提交或截图。`.env.development.local` 及临时密钥文本属于本地配置。
- 暂缓的是最后一轮的新查找/表格增强、缓存管理重做、加载/性能专项。不要误删此前已做的横向滑动、固定列和缓存弹窗视觉适配。
- 本地开发、Git 提交/推送和部署是不同动作。2026-09-12 用户在记录完成后明确授权本轮提交、推送和前端部署，按现有 `main` 工作流发布；此授权不延伸到后端或数据库修改。实际发布状态见修改记录。

## 验证与记录

- 公式或显示相关改动可运行：`node --test tests/calculator-formula.test.mjs tests/calculator-display.test.mjs`。
- 按变更需要运行 `npm run build`、`git diff --check`，并检查相关页面和尺寸。不要把浏览器尺寸模拟写成真实手机全覆盖验收。
- 更新本轮之后的新需求时，在 `CHANGELOG.md` 追加实际变更、验证和状态；重要设计决定写入对应阶段记录。不要仅留下建议编号或把尚未实施的事项记成完成。
